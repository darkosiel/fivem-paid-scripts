config = {
    mainIdenfitier = 'steam', -- the identifier that you use to store user data (steam, license, discord, ...)
    exitCoords = vec4(-75.29, -819.08, 326.18, 0.19),
    finishOnDisconnect = true, -- if true, the duel will finish when a player disconnects
    clearStatsOnRestart = false -- if true, the stats will be cleared when the server/script restarts
}